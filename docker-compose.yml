version: "3.8"
services:
  taskforge:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - taskforge-data:/data
    environment:
      - NODE_ENV=production
      - PORT=4321
      - DATABASE_URL=file:/data/taskforge.db
      - CORS_ORIGIN=*

volumes:
  taskforge-data:
