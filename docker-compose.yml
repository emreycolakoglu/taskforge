version: "3.8"
services:
  taskforge:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - taskforge-data:/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=file:/data/taskforge.db
      - CORS_ORIGIN=*

volumes:
  taskforge-data:
